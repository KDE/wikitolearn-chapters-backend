version: '3'
services:
  persistence-chapters-app:
    build:
      context: ./src/
      args:
        SERVICE_PORT: ${SERVICE_PORT}
    image: wikitolearn/chapters
    ports:
    - "${SERVICE_PORT}:${SERVICE_PORT}"
    environment:
    - MONGO_HOST=${MONGO_HOST}
